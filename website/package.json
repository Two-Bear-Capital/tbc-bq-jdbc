{
  "name": "tbc-bq-jdbc-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "api": "cd .. && ./mvnw -q -DskipTests javadoc:javadoc -B && mkdir -p website/public/api && cp -r \"$(find target -type d -name apidocs | head -1)\"/. website/public/api/",
    "predev": "node scripts/sync-docs.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/sync-docs.mjs",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.3",
    "astro": "^7.1.3",
    "sharp": "^0.33.5"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
